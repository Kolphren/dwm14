import 'package:flutter/material.dart';

class UpdateDetailScreen extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Update Detail'), centerTitle: true,
        ),
        body: SafeArea(child: Text('Update Detail')));
  }
}